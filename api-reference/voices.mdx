---
title: "GET /v1/voices/list"
description: Retrieve available voices with filtering and pagination
api: "GET https://api.neospeech.io/v1/voices/list"
---

## Overview

Get a paginated list of all available text-to-speech voices. Filter by gender, locale, or search by name. Access 45+ high-quality voices across multiple languages and accents.

## Authentication

<ParamField header="Authorization" type="string" required>
  Bearer token with your API key: `Bearer sk-your-api-key-here`
</ParamField>

## Query Parameters

<ParamField query="gender" type="string">
  Filter voices by gender

  **Options:** `male`, `female`, `neutral`
</ParamField>

<ParamField query="locale" type="string">
  Filter voices by language locale

  **Examples:** `en-US`, `en-GB`, `es-ES`, `fr-FR`, `de-DE`, `ja-JP`, `ko-KR`
</ParamField>

<ParamField query="search" type="string">
  Search voices by name or description

  Case-insensitive partial matching
</ParamField>

<ParamField query="limit" type="integer" default="20">
  Number of results per page

  **Range:** 1 to 100
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Number of results to skip for pagination

  **Minimum:** 0
</ParamField>

## Response

### Success Response

**Status Code:** `200 OK`

**Content-Type:** `application/json`

```json
{
  "success": true,
  "data": {
    "voices": [
      {
        "id": "lyra",
        "name": "Lyra",
        "gender": "female",
        "locale": "en-US",
        "language": "English (US)",
        "description": "Warm, professional American female voice",
        "tags": ["professional", "friendly", "clear"],
        "sample_url": "https://cdn.neospeech.io/samples/lyra.mp3"
      },
      {
        "id": "kai",
        "name": "Kai",
        "gender": "male",
        "locale": "en-US",
        "language": "English (US)",
        "description": "Confident, energetic American male voice",
        "tags": ["energetic", "confident", "dynamic"],
        "sample_url": "https://cdn.neospeech.io/samples/kai.mp3"
      }
    ],
    "pagination": {
      "total": 47,
      "limit": 20,
      "offset": 0,
      "returned": 20
    }
  }
}
```

<ResponseField name="success" type="boolean">
  Request status indicator
</ResponseField>

<ResponseField name="data" type="object">
  Voice list response data

  <Expandable title="data properties">
    <ResponseField name="voices" type="array">
      Array of voice objects

      <Expandable title="voice properties">
        <ResponseField name="id" type="string">
          Unique voice identifier (use this in TTS requests)
        </ResponseField>

        <ResponseField name="name" type="string">
          Human-readable voice name
        </ResponseField>

        <ResponseField name="gender" type="string">
          Voice gender: `male`, `female`, or `neutral`
        </ResponseField>

        <ResponseField name="locale" type="string">
          Language locale code (e.g., `en-US`, `fr-FR`)
        </ResponseField>

        <ResponseField name="language" type="string">
          Full language name with region
        </ResponseField>

        <ResponseField name="description" type="string">
          Voice characteristics and best use cases
        </ResponseField>

        <ResponseField name="tags" type="array">
          Descriptive tags for voice characteristics
        </ResponseField>

        <ResponseField name="sample_url" type="string">
          URL to audio sample of the voice
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="pagination" type="object">
      Pagination metadata

      <Expandable title="pagination properties">
        <ResponseField name="total" type="integer">
          Total number of voices matching filters
        </ResponseField>

        <ResponseField name="limit" type="integer">
          Requested page size
        </ResponseField>

        <ResponseField name="offset" type="integer">
          Current pagination offset
        </ResponseField>

        <ResponseField name="returned" type="integer">
          Number of voices in current response
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

### Error Responses

<AccordionGroup>
  <Accordion title="400 Bad Request">
    Invalid query parameters

    ```json
    {
      "success": false,
      "message": "Invalid 'limit' parameter. Must be between 1 and 100."
    }
    ```
  </Accordion>

  <Accordion title="401 Unauthorized">
    Missing or invalid API key

    ```json
    {
      "success": false,
      "message": "Authorization header is missing or invalid."
    }
    ```
  </Accordion>

  <Accordion title="500 Internal Server Error">
    Server error retrieving voices

    ```json
    {
      "success": false,
      "message": "Failed to retrieve voice list",
      "retryable": true
    }
    ```
  </Accordion>
</AccordionGroup>

## Examples

<CodeGroup>

```bash cURL - All Voices
curl -X GET "https://api.neospeech.io/v1/voices/list" \
  -H "Authorization: Bearer sk-your-api-key-here"
```

```bash cURL - Filtered
curl -X GET "https://api.neospeech.io/v1/voices/list?gender=female&locale=en-US&limit=10" \
  -H "Authorization: Bearer sk-your-api-key-here"
```

```javascript JavaScript
const apiKey = process.env.NEOSPEECH_API_KEY;

async function listVoices(filters = {}) {
  const params = new URLSearchParams(filters);
  const url = `https://api.neospeech.io/v1/voices/list?${params}`;

  const response = await fetch(url, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${apiKey}`
    }
  });

  if (!response.ok) {
    const error = await response.json();
    throw new Error(error.message);
  }

  const result = await response.json();
  return result.data;
}

// Get all voices
const allVoices = await listVoices();
console.log(`Found ${allVoices.pagination.total} voices`);

// Filter by gender and locale
const femaleUSVoices = await listVoices({
  gender: 'female',
  locale: 'en-US'
});

// Search by name
const searchResults = await listVoices({
  search: 'professional'
});
```

```python Python
import requests
import os

def list_voices(filters=None):
    params = filters or {}
    response = requests.get(
        'https://api.neospeech.io/v1/voices/list',
        headers={
            'Authorization': f'Bearer {os.getenv("NEOSPEECH_API_KEY")}'
        },
        params=params
    )

    if response.status_code == 200:
        result = response.json()
        return result['data']
    else:
        error = response.json()
        raise Exception(error.get('message', 'Request failed'))

# Get all voices
all_voices = list_voices()
print(f'Found {all_voices["pagination"]["total"]} voices')

# Filter by gender and locale
female_us_voices = list_voices({
    'gender': 'female',
    'locale': 'en-US'
})

# Search by name
search_results = list_voices({
    'search': 'professional'
})
```

</CodeGroup>

## Pagination Example

Retrieve all voices with pagination:

<CodeGroup>

```javascript JavaScript
async function getAllVoices() {
  const allVoices = [];
  let offset = 0;
  const limit = 50;

  while (true) {
    const result = await listVoices({ limit, offset });
    allVoices.push(...result.voices);

    console.log(`Retrieved ${allVoices.length} of ${result.pagination.total}`);

    if (allVoices.length >= result.pagination.total) {
      break;
    }

    offset += limit;
  }

  return allVoices;
}
```

```python Python
def get_all_voices():
    all_voices = []
    offset = 0
    limit = 50

    while True:
        result = list_voices({'limit': limit, 'offset': offset})
        all_voices.extend(result['voices'])

        print(f'Retrieved {len(all_voices)} of {result["pagination"]["total"]}')

        if len(all_voices) >= result['pagination']['total']:
            break

        offset += limit

    return all_voices
```

</CodeGroup>

## Voice Selection Tips

<CardGroup cols={2}>
  <Card title="Use Case Matching" icon="bullseye">
    Choose voices based on description and tags for your specific use case
  </Card>
  <Card title="Test Samples" icon="play">
    Listen to sample_url before selecting a voice for production
  </Card>
  <Card title="Locale Matching" icon="globe">
    Match voice locale to your content language for best pronunciation
  </Card>
  <Card title="A/B Testing" icon="flask">
    Test multiple voices with your audience to find the best fit
  </Card>
</CardGroup>

## Popular Voice Categories

### Professional Voices
Perfect for business presentations, tutorials, and corporate content
- Lyra (female, en-US) - Warm professional tone
- Marcus (male, en-US) - Authoritative and clear
- Victoria (female, en-GB) - Sophisticated British accent

### Conversational Voices
Great for chatbots, virtual assistants, and casual content
- Kai (male, en-US) - Energetic and friendly
- Zara (female, en-US) - Approachable and warm
- Oliver (male, en-GB) - Casual British tone

### Multilingual Voices
Support for international audiences
- Amélie (female, fr-FR) - French native speaker
- Hans (male, de-DE) - German native speaker
- Yuki (female, ja-JP) - Japanese native speaker
- Sofia (female, es-ES) - Spanish native speaker

## Filter Best Practices

### By Gender
```javascript
// Get all female voices
const femaleVoices = await listVoices({ gender: 'female' });
```

### By Locale
```javascript
// Get all British English voices
const britishVoices = await listVoices({ locale: 'en-GB' });
```

### By Search
```javascript
// Search for professional voices
const professionalVoices = await listVoices({ search: 'professional' });
```

### Combined Filters
```javascript
// Professional American female voices
const specificVoices = await listVoices({
  gender: 'female',
  locale: 'en-US',
  search: 'professional'
});
```

## Rate Limits

| Plan | Requests per Minute |
|------|---------------------|
| Free | 30 |
| Pro | 60 |
| Business | 120 |

<Info>
Voice list requests do not consume credits
</Info>

## Related Resources

<CardGroup cols={2}>
  <Card title="Generate Speech" icon="waveform" href="/api-reference/speech">
    Use voices to create audio
  </Card>
  <Card title="Voice Guide" icon="book" href="/concepts/voices">
    Learn about voice selection
  </Card>
  <Card title="Models" icon="brain" href="/api-reference/models">
    View available audio models
  </Card>
  <Card title="Best Practices" icon="lightbulb" href="/guides/best-practices">
    Optimize voice usage
  </Card>
</CardGroup>